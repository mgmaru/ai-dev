# notes

## ドキュメント概要
AI時代のソフトウェア開発方針を考える上で思ったことなどをメモの粒度で残していく。

## 反省
### 2026/8/1
#### 思ったこと
- 今、どこの会社でも必要なことはAIを使用した開発体制、方針の内製化であると感じた。
- もちろん、プロジェクト固有の機密情報を全社に共有するべきではないが、開発方法、開発体制を整えることは急務であると考える。
- 例えば、**MCPやSkillsを社内のエンジニアに共有することができる仕組み作り**というのはとても大切なことであると感じた。
- 大切なのは、このような**MCPやSkillsに簡易的にアクセスできる**ということ。
- ただし、注意点として、**公開範囲は指定する必要はある。**
  - プロジェクト固有の機密情報を外部に漏らしてはいけない。
- あとは、**社内のAPIの管理体制を整えることが必要**であると考える。
  - 社内にどのようなAPIが存在し、それは利用できるのかどうか。公開範囲は？
  - そのような情報にアクセスでき、必要な時に利用できる仕組みが大切である。
- 多分、この領域（API、MCP、Skillsの管理システムの設計）は現状人間にしかできない。
  - 不必要なAPIを雑多に管理するシステム（雑なシステム）はAIでも簡単に構築できる。
  - ただし雑なシステムはダメだ。**セキュリティを考えたしっかりしたシステムでなければいけない。**
  - あるプロジェクトで使用したMCP、Skillsが便利で使えたからといって、そのまま社内のエンジニアに共有できるわけではない。
  - **必ず、人間による確認が必要**であると考える。
- 以上が私のAI時代の開発体制の構築、内製化が必要であるいう考えであるが、いずれの考えも共通の目的がある。
- それは、**開発者のUXを上げること。**これが現状、どの会社にも求められる課題なんだと思う。
---
### 2026/9/7
#### やったこと
- CodexのSkillのベストプラクティスの調査
- ハーネス調査
#### 学んだこと
1. Skillのベストプラクティス
- 常に必要な**知識**は`AGENTS.md`
- 限定的に必要な**手順**は`SKILL.md`
  - 作成場所：`.agents/skill/<skill name>/SKILL.md`
  - 観点などの参照ドキュメント：`.agents/skill/<skill name>/reference/`
- Skillの`despription`に書くべきこと
  - いつ使うか
  - **いつ使わないか** -> これが抜けがち
- 例：逆に「N+1 クエリを見逃すな」「顧客データをログに出すな」のような判断基準はどこに書くか？
  - これははSkillに書かない方が良い。
  - そもそも、「N+1 クエリを見逃すな」「顧客データをログに出すな」は、**実装時に守られているべきこと**
  - だから、Skillにするより前に、機械的に判断できるのであればlint、もしくはAGENTS.mdなどに書く。
2. `docs/`と`.agents/skill-name/reference`に置くドキュメントの違い
- **所有者が異なる**
  - `docs/`：リポジトリが所有者
  - `reference/`：スキルが所有者
- **読者が異なる**
  - `docs/`：人間、エージェント、
  - `reference/`：そのスキルが動いている時のエージェント
- **内容が異なる**
  - `docs/`：リポジトリの真実
  - `reference/`：その作業の進め方に必要な資料
- **配布が異なる**
  - `docs/`：リポジトリと一緒。
  - `reference/`：スキル/プラグインと一緒に持ち出せる
- 基本的に、個人開発では`/reference`に置くのではなく、`docs/`において、SKILL.mdから参照する形にすると良い。
3. ハーネスとは何か？
- ハーネス定義：**モデル自体でないコード、設定、実行ロジックのすべて**
- **内部ハーネス**と**外部ハーネス**
  - 内部ハーネス：モデルの外側のエージェントの作り手が意識するもの
    - 具体例：ReAct ループ、コンパクション、サンドボックス実装
  - 外部ハーネス：エージェントにフィードフォワードとフィードバックを与える環境。
    - 具体例：AGENTS.md、docs/、CI、リンター、検証環境
  - **エージェントを使う側の人が意識するべきなのは外部ハーネス**であり、近年、**ハーネスエンジニアリング**が流行っている。
4. ハーネスエンジニアリングとは？
- **ハーネスエンジニアリング：AI エージェントが同じミスをしないように解決策を設計すること**
5. 外部ハーネスの具体的なやり方
-  AGENTS.md は百科事典ではなく目次：大きな AGENTS.md を試すと失敗する。
  - 巨大な指示ファイルがタスクやコードを圧迫してエージェントが重要な制約を見落とす。
  - すべてが「重要」だと何も重要でなくなる。
  - 分厚いマニュアルは古びた規則の墓場になる。
  - 単一の塊は機械的なチェックに向かず乖離が不可避。
- エージェントに見えないものは存在しない
  - 認識できるのはリポジトリ内でバージョン管理されたアーティファクト（コード、マークダウン、スキーマ、実行可能プラン）だけ。
  - これ以外は知らないものとしてエージェントは作業を実行する。 
  - **見えるようにするべきもの、見えなくて良いもの、見えてはいけないものを設計**する。
    1. 見えるべきもの：判断の根拠となる知識で繰り返し必要になるもの。
      - 具体例：「なぜ、このライブラリを選んだのか」「このAPIの後方互換を許してはいけない理由」「過去にこの方式で失敗した」
      - これらの知識を`docs/`に置く。
    2. 見えなくて良いもの：一度きりの会話。個人の作業メモ。まだ決まっていない検討中の話。
    3. そもそも見せない方が良いもの
      - 認証情報、顧客データ、外部に出せない情報
      - ここは意図的に見えない状態にする。
- **検証ループを作る（中核）**
  1. Git ワークツリーごとにアプリを起動できるようにして、Codex が変更ごとにインスタンスを立てて操作できるようにする。
    - Chrome DevTools Protocol をエージェントのランタイムに組み込んで DOM スナップショット・スクリーンショット・ナビゲーションのスキルを作り、バグの再現と修正の検証を直接やらせている。
    - ログ・メトリクス・トレースもワークツリーごとの一時的なローカル監視スタック経由で公開し、LogQL や PromQL でクエリできるようにしている。
- **不変条件を機械的に強制する**
  - エージェント生成コードの一貫性は保つことができないことを前提とする。
  - **実装を細かく管理するのではなく、不変条件を徹底**する。
  1. 各ドメインを固定レイヤーに分割し依存方向を厳密に検証
  2. カスタムリンター
  3. 構造テスト
  - テクニックとして、**エラーメッセージに修復手順を書き込んでエージェントのコンテキストに注入し、エージェントが違反した時に、どこまでどう直すかまで教える。**
- **ドキュメントの陳腐化も機械化する**
  - 専用のリンターと CI ジョブでナレッジベースが最新かつ相互参照され構造化されていることを検証する。
  - 定期実行の「ドキュメント整備」エージェントが実際のコード動作を反映していない陳腐化したドキュメントをスキャンして修正 PR を開く。
- **エージェントが詰まったら環境を疑う**
  - **どのような機能が不足しているのか、それをエージェントにとって理解可能かつ実行可能にするにはどうすればよいのかを問いかけることが大事。**
#### 課題
- ハーネスエンジニアリングとして意識する点として、lintやCIなど機械的に任せることができるものは任せた方が良いと思った（Skillにするよりも前に考える）。 -> 不変条件を機械的に強制する
- ハーネスエンジニアリングの中で、現状の自分でできていないことがあると思った。
  - 依存関係の確認（レイヤー分けおよび依存方向の定義は設計でできている気がする）　-> 機械的にできれば良い。
  - カスタムリンター
  - 構造テスト
  - エラーメッセージに修復手順を書き込んでエージェントに参照させる。
  - ドキュメントの陳腐化の防止（専用リンター、CI）
  - ドキュメント整備エージェントの配置
#### 疑問
- 依存方向の確認を機械的にできるツールはあるか？
- カスタムリンターとは何か？
- 構造テストとは何か？
- エラーメッセージとそれに対する修復
- ドキュメントの陳腐化を防止できる専用リンターやCIとはどのようなものか？
- 見なくても良いフォルダを指示する時は、どこに入れるべきか？AGENTS.mdかSkillか？
#### 反省
- ハーネスエンジニアリングはエージェントに間違いをなくすように環境を整えることだということがわかった。
- しかし思ったのは、モデルの進化によってこの間違いすらもモデルが解決してしまうような気がした。
  - わざわざハーネスを組まなくても、自律的に解決してしまうような気がした。
  - エラーメッセージとそれに対する対処法をコンテキストに注入するとう方法は、モデルの進化によって不要になるのではないかと思った。
- **ドキュメントの陳腐化（例えば、ドキュメント内容と実装の乖離など、コードが最新を行き、ドキュメントが古くなって行く場合など）を防ぐ方法を考える。**
- 今後はハーネスエンジニアリングもそうだが、**いかにしてトークン消費を抑えながらエージェントのタスクを実行するかを考える仕組みを考えることが大切**であると思った。
---
