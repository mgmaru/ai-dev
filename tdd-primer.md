# TDD（テスト駆動開発）入門

> 前資料『AI時代の開発ドキュメント設計ガイド』の **第7章の前に読むべき前提知識**
> TDDとは何か／どういう手法か／他の手法との比較／メリット・デメリット／実証研究が本当に言っていること

---

## 目次

1. [TDDとは何か](#1-tddとは何か)
2. [Red-Green-Refactorの実際](#2-red-green-refactorの実際)
3. [手を動かす例](#3-手を動かす例)
4. [TDDの中にある2つの流派](#4-tddの中にある2つの流派)
5. [周辺手法との比較](#5-周辺手法との比較)
6. [メリット](#6-メリット)
7. [デメリットと批判](#7-デメリットと批判)
8. [実証研究は何と言っているか](#8-実証研究は何と言っているか)
9. [向いている場面・向いていない場面](#9-向いている場面向いていない場面)
10. [AIエージェント時代のTDD](#10-aiエージェント時代のtdd)
11. [TDDに入る前に終えておくべき上流工程](#11-tddに入る前に終えておくべき上流工程)
12. [TDDを回すための環境・技能の前提](#12-tddを回すための環境技能の前提)
13. [学習の順序と最初の一歩](#13-学習の順序と最初の一歩)
14. [まとめ](#14-まとめ)

---

## 1. TDDとは何か

### 1.1 一行の定義

> **TDD（Test-Driven Development）とは、実装コードを書く前に、失敗するテストを先に書き、それを通すコードを書き、整えるという短いサイクルを繰り返す開発手法。**

学術的な定義では **「テストと交互に短いコーディングサイクルを繰り返す技法。開発者はまず望む機能のユニットテストを書き、続いて必要な実装コードを書き、リファクタリングする」** とされています。

### 1.2 誰が、いつ

| 項目 | 内容 |
|---|---|
| **提唱者** | Kent Beck |
| **時期** | 1990年代後半、**Extreme Programming（XP）の一部**として |
| **代表書籍** | *Test-Driven Development: By Example*（2002/2003） |
| **前身** | Beck が Smalltalk 用に書いた **SUnit**。これが JUnit / NUnit の祖先になった |

Beck自身は、TDDを始めた動機について **「もしアイデアが明らかに悪く見えるなら、安く試す方法を見つけろ。悪くないと分かればとても面白いことになるから」** という自分のヒューリスティックに従っただけだと語っています。そして **「大きな問題に直面すると不安になり圧倒されるが、TDDは問題を小さく分割させてくれるのでそれを解決してくれる」** とも述べています。

> 💡 **重要**：TDDは「テスト手法」として生まれたのではなく、**「不安を管理し、設計を導くための開発リズム」**として生まれた。ここを外すとTDDの理解を誤る。

### 1.3 Beckの2つのルール

TDDの中核は驚くほど短く要約されます。

> **① 自動化されたテストが失敗した場合にのみ、新しいコードを書く**
> **② 重複を排除する**

この2つから、あとで述べるRed-Green-Refactorのサイクルが導出されます。

### 1.4 Uncle Bob（Robert C. Martin）による3つの法則

より運用寄りに言い換えたものが有名です。

| 法則 | 内容 |
|---|---|
| **第1法則** | 失敗するテストを書くまで、実装コードを書いてはならない |
| **第2法則** | 失敗させるのに十分なだけのテストしか書いてはならない（コンパイルエラーも失敗である） |
| **第3法則** | 現在失敗しているテストを通すのに十分なだけの実装しか書いてはならない |

この3つを守ると、テストと実装が**数十秒〜数分単位で交互に行き来する**ことになります。

---

## 2. Red-Green-Refactorの実際

### 2.1 サイクル

```
        ┌──────────────────────────────┐
        │                              │
        ▼                              │
   ┌─────────┐                         │
   │  RED    │  失敗するテストを書く     │
   │  🔴     │  （まだ実装がないので当然赤）│
   └────┬────┘                         │
        │                              │
        ▼                              │
   ┌─────────┐                         │
   │ GREEN   │  テストを通す最小限の      │
   │  🟢     │  実装を書く（汚くてよい）  │
   └────┬────┘                         │
        │                              │
        ▼                              │
   ┌─────────┐                         │
   │REFACTOR │  緑を保ったまま実装を整える │
   │  🔵     │  （重複排除・命名改善）    │
   └────┬────┘                         │
        │                              │
        └──────────────────────────────┘
              1周＝数十秒〜数分
```

### 2.2 各ステップの意味

| ステップ | やること | やってはいけないこと |
|---|---|---|
| **🔴 Red** | 「こう動いてほしい」をテストとして書く。**実行して赤を確認する** | いきなり複数機能ぶんのテストを書く |
| **🟢 Green** | **とにかく最速で緑にする**。ベタ書き・決め打ちでもよい | ここで綺麗に書こうとする（設計は次のステップ） |
| **🔵 Refactor** | 重複を消す、名前を直す、構造を整える。**テストは緑のまま** | 振る舞いを変える。テストを増やす |

> ⚠️ **初学者が最も間違えるのは Green の段階で綺麗に書こうとすること。**
> TDDは「動かす」と「綺麗にする」を**意図的に分離する**手法です。同時にやると両方失敗する、という経験則に基づいています。

### 2.3 Greenに至るための3つの戦略（Beck）

| 戦略 | 内容 | 使いどころ |
|---|---|---|
| **Fake It（偽装する）** | まず**定数を返す**だけで緑にする | 何を書けばいいか分からないとき |
| **Triangulation（三角測量）** | テストを2つ目、3つ目と足して、**定数では通らなくして**一般化を強制する | 一般化の方向が見えないとき |
| **Obvious Implementation（明白な実装）** | 分かりきっている場合はいきなり本実装を書く | 実装が自明なとき（大半はこれ） |

Fake It は最初かなり奇妙に感じますが、**「テスト自体が正しいことを先に確認する」**という重要な役割があります。テストが常に緑（＝実は何もテストしていない）という事故を防げます。

### 2.4 なぜ「赤を必ず確認する」のか

**書いたテストが本当に失敗することを確認していないテストは、テストとして機能している保証がない**からです。

```
✗ よくある事故
  - assert文の書き忘れ → 常に緑
  - テストランナーが対象を拾っていない → そもそも実行されていない
  - 条件式が常にtrue → 何も検証していない

→ 「一度も赤を見ていないテスト」はこれらを検出できない
```

---

## 3. 手を動かす例

「最安値を求める」という単純な機能で、1周ずつ追ってみます。

### 🔴 Red — 1周目

```go
func Test_出品が1件のとき_その価格が最安値になる(t *testing.T) {
    listings := []Listing{{Price: 3980}}
    got := LowestPrice(listings)
    assert.Equal(t, Yen(3980), got)
}
```

→ `LowestPrice` が存在しないのでコンパイルエラー。**これも「赤」です**（第2法則）。

### 🟢 Green — 1周目（Fake It）

```go
func LowestPrice(listings []Listing) Yen {
    return 3980   // ベタ書き。これでいい。
}
```

→ 緑。ここで「そんな実装は意味がない」と思うのが自然ですが、**テストが正しく動くことが確認できた**という成果があります。

### 🔴 Red — 2周目（Triangulation）

```go
func Test_複数の出品があるとき_最も安い価格を返す(t *testing.T) {
    listings := []Listing{{Price: 3980}, {Price: 2980}, {Price: 4500}}
    got := LowestPrice(listings)
    assert.Equal(t, Yen(2980), got)
}
```

→ 赤。**定数返しでは通らなくなった** ＝ 一般化が強制される。

### 🟢 Green — 2周目

```go
func LowestPrice(listings []Listing) Yen {
    lowest := listings[0].Price
    for _, l := range listings {
        if l.Price < lowest {
            lowest = l.Price
        }
    }
    return lowest
}
```

→ 緑。

### 🔴 Red — 3周目（境界値）

```go
func Test_出品が0件のとき_値なしを返す(t *testing.T) {
    got, ok := LowestPrice(nil)
    assert.False(t, ok)
}
```

→ 赤（現状はパニックする）。**ここで初めて「0件のとき何を返すか」という設計判断を迫られる**。

### 🟢🔵 Green → Refactor

```go
func LowestPrice(listings []Listing) (Yen, bool) {
    if len(listings) == 0 {
        return 0, false
    }
    lowest := listings[0].Price
    for _, l := range listings[1:] {
        if l.Price < lowest {
            lowest = l.Price
        }
    }
    return lowest, true
}
```

### この例が示していること

> **3周目で「0件のときどうするか」という仕様の穴が見つかった。**
> 実装から書いていたら、`listings[0]` と書いた時点で無意識に「1件以上ある」と仮定し、その仮定はどこにも記録されないままリリースされていた可能性が高い。

**TDDが設計手法だというのは、こういう意味です。** テストを先に書くと、**呼び出し側の視点に立たされ、境界条件を先に考えることを強制される**。

---

## 4. TDDの中にある2つの流派

TDDと一口に言っても、**内部で明確に流派が分かれています**。ここを知らないと議論が噛み合いません。

| | **古典派（Detroit / Chicago / Classicist）** | **ロンドン派（London / Mockist）** |
|---|---|---|
| **源流** | Kent Beck の教え | Steve Freeman & Nat Pryce『Growing Object-Oriented Software, Guided by Tests』 |
| **検証の対象** | **状態・出力**（state-based / output-based） | **相互作用**（interaction-based） |
| **モックの使用** | 最小限。自分が制御できるコードはモックしない | 積極的。**あらゆるユニットを完全に隔離する** |
| **設計の進め方** | 振る舞いを書きながら**抽象を自然に創発させる** | **外側から内側へ（outside-in）**。インターフェースを発見していく |
| **リファクタリング耐性** | 高い（内部構造を変えてもテストが壊れにくい） | 低い（内部の呼び出し順が変わると壊れる） |

### 4.1 よくある誤解

「古典派＝内側から外側へ（inside-out）」という説明が広まっていますが、**これはロンドン派コミュニティが自分たちとの差別化のために作った呼び方**で、古典派の実像を正確に表していません。古典派は開発の起点を特定の場所に規定しておらず、**エンドツーエンドの振る舞いに焦点を当てながら、必要に応じてコードを洗練させ、抽象を前もって強制するのではなく自然に創発させる**アプローチです。

### 4.2 ロンドン派への批判

実務者からの報告として、こういうものがあります。

> ロンドン派TDDを試みたチームでは、常にコンポーネント間（および実装コードとテスト間）の**極端に密な結合**に行き着いた。**ロンドン派はモックというあらゆるコードをテスト可能にする仕組みを提供するため、設計に対して事実上いかなる力も及ぼさなかった。**

Kent Beck自身も **モックをあまり使わず、モックがモックを返しモックを返すような悪いユニットテストのコードを見てきた。それはリファクタリングを可能にするどころか妨げるので、まったく良い実践ではない** と述べています。

Martin Fowlerも同じく、あまりモックを使わないが、優れた開発者の中には使う人もいる、という立場です。

> 💡 **初学者への推奨：まずは古典派から。** モックを多用するスタイルは、後述の「test-induced design damage」の主要な発生源でもあります。**自分が制御できるコードはモックしない**、というのが安全な出発点です。

---

## 5. 周辺手法との比較

### 5.1 全体マップ

```
                        「いつテストを書くか」
                ┌──────────────┴──────────────┐
             実装より前                     実装より後
                │                             │
        ┌───────┼───────┐                    │
        │       │       │                    │
       TDD    ATDD     BDD                  TLD
     (ユニット) (受入)  (振る舞い)        (Test-Last)


                        「何で契約を表現するか」
                ┌──────────────┼──────────────┐
              テスト           型            契約表明
                │              │               │
              TDD          型駆動開発      Design by Contract
                          (Type-Driven)     (事前/事後条件)
                │
        ┌───────┴───────┐
      具体例          性質
        │              │
    通常のテスト   Property-Based Testing
```

### 5.2 比較表

| 手法 | 誰が書くか | 何を書くか | 粒度 | 主な狙い |
|---|---|---|---|---|
| **TLD（Test-Last）** | 開発者 | ユニットテスト | 小 | 回帰防止。**設計への影響はない** |
| **TDD** | 開発者 | ユニットテスト（実装前） | 小 | **設計の駆動** ＋ 回帰防止 |
| **ATDD** | 開発者＋QA＋ビジネス | 受け入れテスト（実装前） | 大 | **要求の正確な捕捉**と合意 |
| **BDD** | 開発者＋ビジネス | 振る舞いのシナリオ（Given-When-Then） | 大 | **共通言語**による認識合わせ |
| **Property-Based Testing** | 開発者 | 満たすべき**性質**（例：ソート後は常に昇順） | 中 | 入力空間の網羅。**人間が思いつかないケース**の発見 |
| **Design by Contract** | 開発者 | 事前条件・事後条件・不変条件 | 中 | 契約違反の**実行時検出** |
| **型駆動開発** | 開発者 | 型（不正な状態を表現不可能にする） | 中 | **コンパイル時**の保証 |
| **SDD（仕様駆動開発）** | 人間＋AI | 構造化された仕様書 | 大 | AIエージェントへの**意図の伝達** |

### 5.3 TDD / ATDD / BDD の関係

この3つは**競合ではなく階層**です。

```
┌──── ATDD / BDD ────────────────────────┐
│  「顧客が期待する振る舞いは何か」          │
│  ビジネスの言葉。ステークホルダーと合意     │
│                                        │
│   ┌──── TDD ──────────────────┐        │
│   │  「このコードは正しく動くか」  │        │
│   │  開発者の言葉。実装単位       │        │
│   └───────────────────────────┘        │
└────────────────────────────────────────┘
```

- **TDDは機能性をテストし、BDDはドメインや設計のロジックをテストする**
- **ATDDはTDDを拡張してテスター・開発者・ビジネス関係者の協働を改善するもの**で、受け入れテストケースを開発の基礎に据える
- **BDDは機能の振る舞いにより焦点を当て、ATDDは正確な要求の捕捉に焦点を当てる**という違いがある
- **1つだけを使う必要はなく、プロジェクトの要求に応じて組み合わせられる**

前資料で説明した **Double Loop（外側＝受け入れテスト、内側＝ユニットテスト）** は、まさに **ATDD/BDD ＋ TDD** の組み合わせです。

### 5.4 TDDと型・契約の関係

これは**競合ではなく分担**です。前資料の議論と直結します。

| 表現したいこと | 最適な手段 | 理由 |
|---|---|---|
| 「この状態は存在しえない」 | **型** | コンパイル時に検出。テストより早く、漏れがない |
| 「この入力にはこう応答する」 | **テスト** | 具体例で表現でき、人間が読める |
| 「常にこの性質を満たす」 | **Property-Based Testing** | 個別の例では網羅できない |
| 「呼び出し側はこれを守れ」 | **型 or 契約表明** | 実行時／コンパイル時に強制 |

> 💡 **型で表現できることをテストで書くのは重複。** `Option<T>` を返す関数に「nilを返さないこと」のテストは不要です。**型を効かせるほど、必要なテストの量は減ります。**

---

## 6. メリット

### 6.1 設計面

| メリット | 説明 |
|---|---|
| **呼び出し側から設計できる** | テストは必然的に「使う側」のコードになる。実装の都合ではなくドメインの都合でAPIが決まる |
| **境界条件を先に考えさせられる** | 0件、null、最大値などを実装前に検討することになる |
| **YAGNIが自然に守られる** | 「失敗しているテストを通すのに十分なだけ」しか書かないため、使われない機能が生まれにくい |
| **不安の管理** | Beckの動機そのもの。大きな問題を小さく分割できる |

### 6.2 品質・保守面

| メリット | 説明 |
|---|---|
| **リファクタリングの安全網** | 緑を保ったまま構造を変えられる。**これがないとリファクタリングは博打になる** |
| **回帰の即時検出** | 壊れた瞬間に分かる。数週間後ではなく |
| **テストが自動ドキュメントになる** | Microsoftの事例研究でも、**コードの保守・利用時にテストが自動ドキュメントとして機能した**と報告されている |
| **デバッグ時間の削減** | 失敗したテストが問題箇所を直接指す |

### 6.3 AI協業面（2026年に追加された効能）

| メリット | 説明 |
|---|---|
| **AIのごまかしを防ぐ** | エージェントは**自分の壊れたロジックを検証するテストを書くことでごまかせない**し、**失敗を消すためにアサーションを静かに削除することもできない** |
| **「正しい」の定義が先に確定する** | AIが1行も実装する前に受け入れ基準が決まる |
| **エージェントの停止を防ぐ** | Kent Beck曰く、**このサイクルがないとAIが生成した複雑性が蓄積し、やがてエージェントが完全に停止する** |
| **曖昧さの削減** | 研究上も **テストケースは曖昧さを減らし、実行可能な仕様として機能することで性能を向上させる** |

---

## 7. デメリットと批判

**ここは公平に扱う必要があります。TDDは万能ではなく、明確な批判が存在します。**

### 7.1 コスト面

| デメリット | 実測値・根拠 |
|---|---|
| **初期開発時間が伸びる** | Microsoft / IBM の事例研究で **初期開発時間が15〜35%増加**（主観評価）。Nagappan曰く「12か月の開発サイクルで35%は4か月。これは巨大だ」 |
| **習得コストが高い** | **開発者は設計意図と詳細なテストロジックの両方を維持しなければならず、導入期にはしばしば生産性を下げる** |
| **テスト自体の保守コスト** | テストコードも資産であり負債でもある |

### 7.2 DHH の「TDD is dead」（2014年）

Ruby on Rails 作者の David Heinemeier Hansson による、最も有名な批判です。

**主な論点**

| # | 主張 |
|---|---|
| **1** | ユニットテストの定義が狭すぎる。DBやファイルシステムなど協働者に触れられない |
| **2** | **モック多用のアーキテクチャや hexagonal architecture は「まったく良くない」**。これが **test-induced design damage（テストに起因する設計の損傷）** を生む |
| **3** | Red-Green-Refactor ループの義務化が行きすぎている |
| **4** | TDDと「プロフェッショナリズム」を同一視する言説への反発 |

DHH自身の言葉：

> **現在の熱狂的なTDD経験は、ユニットテストへの一次的な集中をもたらす。それらが（テストファーストの本来の正当化理由である）コード設計を駆動できるテストだからだ。私はそれが健全だとは思わない。**

> **テストファーストのユニットは、「遅いこと」——DBに触れるようなこと——を避けるために、中間オブジェクトと間接層の過度に複雑な網をもたらす。**

DHHの処方箋は「テストをやめろ」ではなく、**「テストのスペクトルをユニットからシステムへ再バランスせよ」**というものでした。

### 7.3 「test-induced design damage」とは何か

具体的にはこういう現象です。

```
「テストしやすくするため」に導入されるもの
        ↓
  ・不要なインターフェース（実装が1つしかないのに）
  ・不要な依存性注入
  ・不要な間接層
        ↓
  テストは通るが、コードは読みにくく、変更しにくくなる
        ↓
  「テストのための設計」が「問題を解くための設計」を侵食する
```

DHHは **「コントローラーをユニットテストするために2〜3層の余分な間接が必要？もちろん価値がある」という事後的正当化**が横行していると批判しました。

> ⚠️ **これは実在するリスクです。** 前資料で述べた「**内部実装に密結合したユニットテストは、変えにくさの純粋な負債**」も同じ問題を指しています。

### 7.4 批判への反論

一方で、この批判には反論もあります。

- Michael Feathers らの古典派の立場では、**モックの過剰使用こそがテストの持つ有益な設計圧力を損なう**。モックは**自分が制御できない依存に対しては妥当だが、制御下のコードには通常使わない**
- DHHの批判は**フレームワーク作者の視点から書かれている**。フレームワーク（RailsやReact）は独自の設計圧力を課すため、アプリケーションドメインの設計圧力と競合しやすい

つまり **「TDDが悪い」のではなく「ロンドン派の極端な適用が悪い」** という整理が可能です。

---

## 8. 実証研究は何と言っているか

**ここが最も重要なセクションです。TDDは信仰の対象になりやすいので、データを見ておく価値があります。**

### 8.1 産業界の事例研究（最も引用される）

**Nagappan et al. (2008)** — Microsoft 3チーム＋IBM 1チーム

| 指標 | 結果 |
|---|---|
| **リリース前欠陥密度** | 類似の非TDDプロジェクトと比較して **40〜90%減少**（IBM: 40%、Microsoft: 60〜90%） |
| **初期開発時間** | **15〜35%増加**（主観評価） |
| **ユニットテストのブロックカバレッジ** | 79〜88% |

**George & Williams (2003)** — プロ開発者24名

| 指標 | 結果 |
|---|---|
| コード品質（ブラックボックステスト合格率） | **18%向上** |
| 開発時間 | **16%増加** |

> 📌 **一貫したパターン：品質は上がる。時間はかかる。**

### 8.2 しかし、学術研究は結論が割れている

**体系的文献レビュー**では、既存のレビュー自体が**さまざまな結果を示している**と報告されています。研究の「厳密性（rigor）」と「関連性（relevance）」で分類しなおすと：

> **厳密性と関連性のスコアが高い研究は、外部品質の改善について明確な結果を示すが、それは生産性の損失を伴うように見える。**

一方で、**Test-Lastチームの方が生産的で、Test-Firstチームより多くのテストを書いた**という、多くの先行研究と異なる結果を出した実験も存在します。

### 8.3 最も示唆的な研究：Fucci et al. (2017)

**これはTDDの理解を変える研究です。**

プロ開発者39名から82のデータポイントを集め、TDDのプロセス特性を4つに分解して回帰分析しました。

| プロセス特性 | 意味 | 品質・生産性への影響 |
|---|---|---|
| **Granularity（粒度）** | 1サイクルの細かさ | ✅ **正の相関（主要因）** |
| **Uniformity（均一性）** | サイクルの長さが揃っているか | ✅ **正の相関（主要因）** |
| **Sequencing（順序）** | テストを先に書くか後に書くか | ❌ **重要な影響なし** |
| **Refactoring effort** | リファクタリングの量 | ❌ 負の相関（mixed refactoringの影響と推測） |

**結論（原文の要旨）**

> **TDDの主張される利益は、その特徴的なテストファーストの力学によるものではなく、TDD的なプロセスが細かく安定したステップを促し、集中とフローを改善するという事実によるものかもしれない。**

### 8.4 これが実務に意味すること

> 🔑 **効いているのは「テストを先に書くこと」そのものではなく、「小さく、一定のリズムで刻むこと」である可能性が高い。**

これは前資料で「粒度が大事」と述べたことの**実証的な裏付け**になっています。そして実践上、非常に重要な含意があります。

```
❌ 教条的な理解
   「テストを先に書かないTDDは偽物だ」
   → 守れないと挫折する

✅ 研究に沿った理解
   「小さく刻み、リズムを保つことが本体。
    テストファーストはそれを実現する優れた道具のひとつ」
   → 部分適用でも効果が出る
```

**ただし注意点**：Fucci らの研究は「テストを後で書いてもよい」と言っているのではありません。**テストを先に書くことは、細かいステップを強制する最も確実な仕組み**です。順序を捨てると、粒度も一緒に失われるのが普通です。

### 8.5 なぜ研究の結論が割れるのか

- **TDDの厳密な定義が存在しない**ため、定量的評価が困難
- 被験者が学生か professional かで結果が変わる
- **タスクの複雑さがTDDの効果を著しく覆い隠す**
- **プロセス遵守度（conformance）が結果を左右する**。遵守度が高いと品質は改善し、生産性は悪化しない

---

## 9. 向いている場面・向いていない場面

### ✅ TDDが特に効く場面

| 場面 | 理由 |
|---|---|
| **ロジックが複雑で、入出力が明確** | 価格計算、日付処理、パース、状態遷移 |
| **仕様が既に決まっている** | 何を作るか分かっているので、テストが先に書ける |
| **バグ修正** | 再現テストを先に書く。**最も費用対効果が高い** |
| **長期保守されるコード** | リファクタリングの安全網が効いてくる |
| **AIエージェントに実装させるとき** | 「正しい」の定義を先に固定できる |

### ⚠️ TDDが向かない・工夫が必要な場面

| 場面 | 理由 | 代替 |
|---|---|---|
| **探索的な実装（spike）** | 何を作るか分かっていないのでテストが書けない | **まず捨てるコードを書いて理解し、その後TDDで作り直す** |
| **UI・レイアウト** | 見れば分かる。テストの費用対効果が悪い | 目視 ＋ スナップショットテスト |
| **外部APIとの結合** | モックしても本物の挙動は分からない | 契約テスト、実物に対する統合テスト |
| **機械学習・数値計算** | 期待値を事前に書けない | Property-Based Testing、許容誤差アサート |
| **プロトタイプ・使い捨てコード** | 保守しないのでコストが回収できない | テストなしでよい |

> 💡 **探索的な仕事におけるTDDの正しい使い方**は、前資料の「垂直スライス」と組み合わせることです。
> **スライス#1で理論を育てる（TDDなしでよい）→ 育った理論を受け入れテストと型に固定する → 以降はTDDで進む。**

---

## 10. AIエージェント時代のTDD

### 10.1 なぜ再評価されているのか

エージェント型コーディングの典型的な失敗パターンがこれです。

```
エージェントが実装コードを書く（もっともらしく見える）
        ↓
「テストも書いて」と頼む
        ↓
エージェントはコードを読み、コードと一致するテストを生成する
        ↓
当然、全部緑になる
        ↓
検証したつもりで、実は何も検証していない（トートロジー）
```

TDDはこれを構造的に防ぎます。**テストが先に存在し、それは自分のものであり、AIの仕事はそれを通すことになる**からです。

### 10.2 Kent Beck の見解

Beckはこの組み合わせを **"superpower"** と呼んでいます。また、AIとの協働を2つに区別しています。

| | 内容 |
|---|---|
| **Vibe coding** | 振る舞いだけを気にする。エラーメッセージをモデルに投げ返し、動くものを受け入れる |
| **Augmented coding** | **コードそのものを気にする**。その複雑性、テスト、カバレッジ |

TDDは後者を可能にする実践です。

### 10.3 実務的な分担（前資料の再掲）

| ステップ | 誰が |
|---|---|
| ① 受け入れテストを書く | **人間が確定させる**（LLMに書かせてレビューして確定でもよい） |
| ② 内側ループ（Red-Green-Refactor） | **LLMに丸ごと回させる** |
| ③ 緑になったら | **人間が差分と受け入れテストをレビュー** |

### 10.4 それでも懐疑論はある

- 「AIが高速にコードを生成するなら仕様に固定すべきだ」という理屈は一見もっともらしいが、**厳格なTDDがエージェント型AIと出会ったとき実際に何が起きるかを見ると、通説より複雑で厄介**という指摘
- エージェントは **以前は通っていたテストをしばしば壊す**。ある研究では、変更前のグラフベース影響分析によって回帰率が 6.08% → 1.82% に低下したと報告されている

> **TDDはAIの誤りをゼロにする魔法ではなく、誤りを「静かに」ではなく「うるさく」失敗させる装置です。**

---

## 11. TDDに入る前に終えておくべき上流工程

> **TDDが要求するのは「完成した要件定義書」ではない。**
> **「これから作る1スライスぶんの、確定した受け入れ基準」である。**

ここを取り違えると、両側に失敗します。

```
❌ 前倒ししすぎ                      ❌ 後回ししすぎ
   全機能の要件定義を完成させてから開始     受け入れ基準なしでTDD開始
        ↓                                ↓
   ウォーターフォールに戻る                そもそもテストが書けない
   理論は作らないと育たないので、           →実装しながらテストを書く
   その要件定義自体が高確率で外れる          →トートロジーになる
```

**正解は二層に分けることです。** これは前資料で扱った Spec Growth Engine の二層ルールと同じ構造です。

```
┌─ 層1：プロジェクト開始時に一度だけ ─────────────────┐
│   ① スコープ（作るもの／作らないもの）                │
│   ② ドメイン用語集（ユビキタス言語）                  │
│   ③ 機能一覧（粗くてよい）                           │
│   ④ アーキテクチャの不変条件                         │
│   ⑤ スライスの順序（難所から）                        │
└──────────────────────────────────────────────────┘
                    ↓
┌─ 層2：各スライスの直前に、毎回 ────────────────────┐
│   ⑥ そのスライスのユーザーストーリー                  │
│   ⑦ そのスライスの受け入れ基準 ← TDDの入力そのもの    │
│   ⑧ 使う外部インターフェースの契約                    │
└──────────────────────────────────────────────────┘
                    ↓
                  TDD開始
```

---

### 11.1 【層1】プロジェクト開始時に一度だけやること

#### ① スコープ：作るもの／作らないもの

**「作らないもの」を明示するのが重要です。** 前資料でも触れたとおり、**明示的な「スコープ外」セクションでエージェントの探索を制限する**のは、AI協業における定石になっています。

```markdown
## スコープ
作る：
- 商品URLの登録と価格の定期取得
- 価格履歴の保存と表示

作らない（今回は）：
- 通知機能
- 複数ユーザー対応
- モバイルアプリ
```

#### ② ドメイン用語集（ユビキタス言語）

**これがTDDに直結します。** 前資料で「テストはドメインの言葉で書く」と述べましたが、**その言葉が定義されていなければ書けません**。

```markdown
## 用語
- 出品（Listing）：ある商品の、あるショップにおける販売単位
- 最安値（LowestPrice）：送料込み価格が判明している出品のうち最小のもの
- 巡回（Crawl）：登録済み全商品の価格を取得する一連の処理
```

実際、AIエージェント用のTDDスキルの設計例でも、**テストを書く前にコンテキスト文書とADRを参照し、テスト名とインターフェースの語彙がプロジェクトのドメイン言語と一致するようにする**、という手順が組み込まれています。

> 💡 **用語集は10〜20語で十分。** 完璧を目指さず、実装しながら追記していきます。

#### ③ 機能一覧（粗くてよい）

**これは「出しておくべき」です。ただし詳細は不要です。**

必要なのは3列だけ。

| 機能 | 優先度 | 難易度・リスク |
|---|---|---|
| 商品URL登録 | 高 | 低 |
| 価格取得（スクレイピング） | 高 | **高**（サイト構造依存） |
| 価格履歴の保存 | 高 | 中 |
| 履歴グラフ表示 | 中 | 低 |
| 通知 | 低 | 中 |

**なぜ必要か**：**⑤のスライス順序を決めるため**です。「難所を先に」を判断するには、何が難所かを俯瞰する必要があります。

**なぜ詳細が不要か**：詳細を書いても、スライス#1を通した時点で前提が変わるからです。詳細設計はスライス直前（層2）で行います。

> ⚠️ **機能一覧を「実装状況リスト」に育てないこと。** 前資料で述べたとおり、恒久的な進捗表は必ず腐ります。**これはスライス順序を決めるための一時的な道具**です。

#### ④ アーキテクチャの不変条件

前資料の二層ルールの層1そのものです。**いかなる機能よりも先に決めます。**

| 決めること | 例 |
|---|---|
| **永続化** | SQLite／ファイル／どこに何を保存するか |
| **セキュリティ** | 認証の有無、秘密情報の扱い |
| **外部連携の境界** | どこがIOで、どこが純粋か（Functional Core / Imperative Shell の線引き） |
| **エラー分類** | Result/Option をどう使うか、リトライするエラーとしないエラー |

**なぜ事前か**：これらは**どの機能も単独では強制しないため、放っておくと永久に現れない**からです。前資料で紹介した「商品リストが312行のハードコード配列になった」失敗がこれです。

**TDDとの関係**：④が決まっていないと、テストの中でDBをどう扱うかが決まらず、**1本目のテストで詰まります**。

#### ⑤ スライスの順序

③の一覧を見て、**最もリスクの高い経路を貫くスライス#1**を決めます。

```
スライス#1：1商品の価格を1回取得してDBに保存し、画面に1行表示する
            ↑ 一番難しい「スクレイピング」を含む。本物で通す
スライス#2：複数商品に対応
スライス#3：履歴を蓄積して表示
```

---

### 11.2 【層2】各スライスの直前にやること

**ここがTDDの直接の入力です。**

#### ⑥ ユーザーストーリー

```
As a <ユーザー>, I want <期待される成果>, so that <その成果のビジネス価値>
```

ストーリーは**解決策ではなく問題の記述**であり、受け入れ基準というスコープに包まれたものだと整理されています。

```
As a 価格を追跡したい人,
I want 登録した商品の現在価格を一覧で見たい,
so that 買い時を判断できる
```

#### ⑦ 受け入れ基準（Acceptance Criteria）— **最重要**

**これがTDDの入力そのものです。これがなければTDDは始められません。**

受け入れ基準は **具体的で曖昧さがなく、開発者が実装の対象とし、品質保証がテストの対象とする基準**である必要があります。

**書き方は2形式ある**

**(a) Given-When-Then（BDD形式）** — ユーザーの操作フローがあるとき

```
Scenario: 価格が取得できた場合
  Given 商品URLが登録されている
  When 巡回を実行する
  Then 一覧に現在価格が税込で表示される
  And 価格履歴に1件追加される

Scenario: 価格が取得できなかった場合
  Given 商品URLが登録されている
  When 巡回で価格要素が見つからない
  Then 一覧に「取得失敗」と表示される
  And 前回価格は保持される
```

**(b) ルール型チェックリスト** — ワークフローに属さない制約のとき

```
- ページの読み込みは2秒以内であること
- /export エンドポイントは status, fileId, url キーを持つJSONを返すこと
- 価格は税込・カンマ区切りで表示すること
```

**個数の目安**

> **受け入れ基準は6〜9個に制限する。多すぎる場合、そのストーリーが大きすぎるか複雑すぎることを示している。10個以上あるなら、複数の小さなストーリーに分割することを検討する。**

> 🔑 **これはそのまま「スライスの大きさの判定基準」として使えます。** 受け入れ基準が10個を超えたら、スライスを割ってください。

**必ず含めるべきもの**

受け入れ基準は **エッジケース、エラー条件、他システムとの結合点にも言及すべき**です。これらは開発中に発見されがちで、スコープ拡大と遅延を招きます。前資料で「LLMが最も落とすのは境界値と異常系」と述べましたが、**そこを事前に書いておくことが最大の防御**になります。

#### ⑧ 外部インターフェースの契約

そのスライスが外部（API、ライブラリ、DB）に触れるなら、**そのレスポンス形式を先に確認します**。

- **分かっている場合**：契約として書き留める
- **分からない場合**：**spikeを打つ**（テストなしの使い捨て実装で確認 → 捨てる → 契約を書く → TDD開始）

---

### 11.3 Definition of Ready — TDDに入ってよいかのゲート

アジャイル開発には **Definition of Ready（準備完了の定義）** という概念があります。

> **ユーザーストーリーやバックログ項目がスプリントに含められる前に満たすべき基準。開発が始まる前に、作業が十分に詳細化され、理解され、実行可能であることを保証する品質ゲート。**

**TDD向けに翻訳したチェックリスト**

| # | 質問 | 満たせないときの対処 |
|---|---|---|
| 1 | このスライスの受け入れ基準を**言葉で書けるか** | 書けるまで考える。それが要件定義 |
| 2 | 受け入れ基準は**6〜9個以内**か | 超えるならスライスを分割 |
| 3 | **異常系・境界値**が受け入れ基準に含まれているか | 追加する |
| 4 | 使う**外部インターフェースの形**が分かっているか | **spikeを打つ** |
| 5 | 使う**ドメイン用語**が定義されているか | 用語集に追記 |
| 6 | このスライスは**アーキテクチャの不変条件（④）**と矛盾しないか | ④を見直すか、スライスを変える |
| 7 | 「完了」がどういう状態か**説明できる**か | 受け入れ基準に落とす |

> **7項目すべてYesなら、TDDを始めてよい。** 1つでもNoなら、そこが上流の宿題です。

**重要な原則**

> スプリントが始まったら、受け入れ基準の変更は避けることが極めて重要である。**それが「何を届けるか」というコミットメントの基礎を成すから。**

TDDに置き換えれば、**受け入れテストを書いたら、実装中にそれを都合よく書き換えない**、ということです。書き換えたくなったら、それは仕様の理解が誤っていたシグナルなので、**一度手を止めて上流に戻ります**。

---

### 11.4 やらなくていい上流工程

**準備しすぎもまた失敗です。以下はTDDの前に不要、というよりTDDと衝突します。**

| よくある「先にやるべき」 | 実際 |
|---|---|
| **全機能の詳細設計** | ❌ 不要。スライス直前でよい。先にやると必ず外れる |
| **全機能の受け入れ基準の洗い出し** | ❌ 不要。次のスライスぶんだけでよい |
| **DBスキーマ全体の確定** | ❌ 不要。ただし**永続化方式（④）は決める**。テーブル定義は育てる |
| **完全な要件定義書** | ❌ 不要。理論は作ることでしか育たない |
| **画面遷移図の全網羅** | △ 主要導線だけで十分 |
| **非機能要件の数値目標** | △ 「検証可能なものだけ」書く。「高速に」は書いても意味がない |

**なぜ前倒しが害になるか**：前資料で扱ったNaurの議論そのものです。**システムの理論は実装を通じてしか構築されない**ため、スライス#1を通す前に書いた詳細設計は、**まだ理論を持たない状態で書かれた推測**にすぎません。

---

### 11.5 全体の流れ

```
【プロジェクト開始】
    │
    ├─① スコープを書く（作る／作らない）
    ├─② ドメイン用語集を作る（10〜20語）
    ├─③ 機能一覧を出す（名前・優先度・難易度の3列）
    ├─④ アーキテクチャの不変条件を決める（永続化・セキュリティ・IO境界・エラー分類）
    └─⑤ スライスの順序を決める（難所から）
    │
    ▼
【各スライスのループ】←──────────────────────┐
    │                                        │
    ├─⑥ ユーザーストーリーを書く                │
    ├─⑦ 受け入れ基準を書く（6〜9個・異常系込み）  │
    ├─⑧ 外部契約を確認（不明ならspike）          │
    │                                        │
    ├─ Definition of Ready チェック（7項目）     │
    │      └─ Noがあれば上流に戻る               │
    ▼                                        │
【TDD】                                       │
    ① 受け入れテストを書く（赤）                 │
    ② 内側ループ Red-Green-Refactor            │
    ③ 受け入れテストが緑 → スライス完了           │
    │                                        │
    ├─ 得られた理論を用語集・不変条件に反映 ───────┤
    │                                        │
    └─ 次のスライスへ ───────────────────────┘
```

**注目してほしいのは最後の還流です。** スライスを1本通すと理論が育つので、**②の用語集と④の不変条件を更新します**。上流は一度で終わりではなく、**スライスごとに少しずつ確定していく**のが正しい姿です。

---

### 11.6 個人開発のための最小セット

「全部やる時間はない」という場合、**これだけあればTDDは始められます**。

```markdown
# docs/context.md   ← 1ファイルで足りる

## スコープ
作る：（3行）
作らない：（3行）

## 用語
- 用語A：定義
- 用語B：定義
（10語程度）

## 決めたこと（不変条件）
- 永続化：SQLite、ファイルは data/app.db
- エラー：想定内は Result で返す。panic しない
- IO境界：外部通信は infra/ 配下のみ。ロジックは純粋関数

## スライス順序
1. （難所を含む1本）
2. ...
```

そして各スライスの直前に、**受け入れ基準を6〜9個、紙かIssueに書く**。これだけです。

> **所要時間の目安：初回1〜2時間、以降は各スライス15分程度。**
> これを飛ばすと、TDDの1本目のテストで必ず手が止まります。

---

### 11.7 まとめ：上流とTDDの境界線

| 問い | 答え |
|---|---|
| 要件定義は終わらせておくべきか | **プロジェクト全体としては不要。次のスライスぶんだけ必須** |
| 機能一覧は出しておくべきか | **✅ 出す。ただし名前・優先度・難易度の3列だけ** |
| 詳細設計は必要か | **❌ 不要。スライス直前に、必要なぶんだけ** |
| 何が絶対に必要か | **① 受け入れ基準（6〜9個・異常系込み）② ドメイン用語 ③ アーキテクチャの不変条件** |
| 分からないときは | **spikeを打つ。それも上流工程の一部** |

> **一行でまとめると：**
> **「全体は粗く、目の前のスライスだけ厳密に」**

---

## 12. TDDを回すための環境・技能の前提

> **TDDが回らない原因の大半は「TDDが難しいから」ではなく「前提が揃っていないから」です。**
> 前提が欠けたままRed-Green-Refactorを始めると、必ずどこかで詰まり、「TDDは自分に合わない」という誤った結論に至ります。

前提は4つの層に分かれます。**上から順に、欠けていると致命的な順**です。

```
┌─ A. 環境の前提 ────────── これがないと物理的に回らない
│    ① 1コマンドでテストが走る
│    ② テストが速い
│    ③ いつでも戻せる（Git）
├─ B. 技能の前提 ────────── これがないと形だけになる
│    ④ テストが書ける
│    ⑤ リファクタリングができる
├─ C. 設計の前提 ────────── これがないとモック地獄になる
│    ⑥ 純粋なロジックと副作用が分離されている
│    ⑦ Seam（継ぎ目）がある ※既存コードの場合
└─ D. 仕事の前提 ────────── これがないとテストが書けない
     ⑧ 何を作るか分かっている
```

---

### 12.1 【A】環境の前提

#### ① 1コマンドでテストが走ること

```bash
go test ./...        # これだけ
npm test             # これだけ
cargo test           # これだけ
```

**環境変数の設定や設定ファイルの書き換えを事前に要求するテストは、そもそもユニットテストの資格がない**とFeathersは述べています。「準備が必要」なテストは走らせなくなり、走らせないテストは腐ります。

エディタから保存時に自動実行される状態まで持っていければ理想です。

#### ② テストが速いこと

**これがTDDの生死を分ける最大の要因**です。

Feathersの基準は容赦がありません。

> **ユニットテストの鍵はスピード。1/10秒以上かかるものは長すぎる。**

そして「大きなテストを頻繁に走らせればいいじゃないか」という発想を明確に否定しています。

> **「大きなテストを用意して、頻繁に走らせればいい」——ブブー。それは無理だ。遅いから。統合テストはユニットテストの代替にならない。**

**なぜ速度がここまで重要か**

```
テストが 0.1秒 → 1サイクル数十秒。リズムが保てる。集中が切れない。
テストが 10秒  → 待つのが面倒になり、まとめて書くようになる → 粒度が粗くなる
テストが 60秒  → 走らせなくなる → TDDは崩壊する
```

前述の Fucci et al. の研究で、効いていたのが**粒度と均一性**だったことを思い出してください。**遅いテストは粒度を直接破壊します。** ここが前提のなかで最も重要な理由です。

**具体的な目安**

| 種類 | 目標時間 | 外部依存 |
|---|---|---|
| ユニットテスト全体 | **数秒以内** | なし（DB・ネットワーク・ファイルI/Oに触れない） |
| 受け入れ/統合テスト | 数十秒〜数分 | あり。ただし**別コマンドで分離**して普段は回さない |

#### ③ いつでも戻せること（Git）

TDDは **「Greenにするために汚いコードを書き、あとで捨てる」** ことを前提にした手法です。捨てられない状態だと、Greenの段階で綺麗に書こうとしてしまい、リズムが壊れます。

- 1サイクルごと、あるいはGreenになるたびにコミットできる状態にしておく
- 迷ったら `git reset --hard` で数分前に戻れる、という安心感が Fake It のような大胆な手を可能にする

---

### 12.2 【B】技能の前提

#### ④ テストが書けること

**当たり前に見えて、ここを飛ばして挫折する人が非常に多い箇所です。**

TDDは「テストを書く技能」＋「順序を逆にする規律」の**2つ**から成ります。前者がないまま後者に挑むと、負荷が二重になります。

**先に身につけておくべき最小セット**

| 項目 | 内容 |
|---|---|
| **Arrange-Act-Assert** | 準備 → 実行 → 検証。テストの基本構造 |
| **アサーションの書き方** | 等値、エラー、パニック、近似値の検証 |
| **テストの命名** | 何を検証しているかが名前で分かること |
| **テーブル駆動テスト** | Goなら特に。入力と期待値の表で書く |
| **テストの独立性** | テストの実行順序に依存しない。共有状態を持たない |

> 💡 **推奨：まずはTest-Last（実装してからテストを書く）で普通にテストを書く練習をする。**
> 順序を変えるのはその後で十分です。テストの書き方に迷っている状態で「先に書く」は無理があります。

#### ⑤ リファクタリングができること

**ここが最も見落とされる前提です。**

Red-Green-**Refactor** の3番目ができないと、TDDは単なる「テスト先書き」になり、**設計改善の効果がゼロになります**。TDDの価値の半分はここにあります。

**リファクタリングの定義を正確に押さえる**

> **リファクタリングとは、コードが何をするかを変えずに、内部構造を改善すること。**
> **書き直しではない。速度を落とすことでもない。新機能をこっそり混ぜることでもない。**

**最低限マスターしておきたい操作**（IDEの自動リファクタリング機能で十分）

- 変数・関数・型の**リネーム**
- **関数の抽出**（Extract Function）
- **変数の抽出/インライン化**
- **重複の除去**
- 引数の追加・削除

> ⚠️ **注意**：Refactorステップで**テストを増やしてはいけません**。振る舞いも変えません。緑を保ったまま構造だけを触る、という制約が守れて初めてリファクタリングです。

**テストが設計のシグナルになる**

リファクタリングの技能が身につくと、テストが設計の問題を教えてくれるようになります。

| テストの症状 | 示唆される設計の問題 |
|---|---|
| テストが書きにくい／保守しにくい | 対象コードの設計が悪い |
| テストのセットアップが重複している | 実装が複雑すぎる |
| 検証に多数のアサーションが必要 | **対象が多くのことをやりすぎている**（単一責任違反） |

---

### 12.3 【C】設計の前提

#### ⑥ 純粋なロジックと副作用が分離されていること

**テスト容易性はテクニックではなく、アーキテクチャから来ます。**

すべてのコードがDB・ネットワーク・グローバル状態に触れている構造では、TDDは物理的に不可能か、**モックだらけになって test-induced design damage を招きます**（§7.3参照）。

**最も費用対効果が高いパターン：Functional Core, Imperative Shell**

```
┌─────────────────────────────────────────────┐
│  Imperative Shell（命令的な殻）              │
│  ・DB、API、ファイル、UI                     │
│  ・副作用を全部ここに集める                   │
│  ・薄く保つ                                  │
│                                             │
│    ┌───────────────────────────────┐        │
│    │  Functional Core（関数的な核）  │        │
│    │  ・純粋なビジネスロジック        │        │
│    │  ・与えられたデータだけで動く    │        │
│    │  ・副作用なし = 決定的           │        │
│    │  ← ここをTDDでガンガン書く      │        │
│    └───────────────────────────────┘        │
└─────────────────────────────────────────────┘
```

- **Functional Core**：与えられたデータのみで動作し、副作用なしに予測可能な出力を生む。**純粋であるがゆえに、隔離した状態で推論もユニットテストも容易**
- **Imperative Shell**：すべての副作用を担い、外部システムとの相互作用を統率する。コアの関数を使ってビジネスロジックを実行し、純粋なロジックと不純な現実の橋渡しをする

**効果**：**コアは決定的で外部依存がないため網羅的にユニットテストでき、コアロジックのために複雑な統合テストを書く必要が大幅に減る。**

**実践的な指針**

```go
// ✗ テストできない。DBとロジックが混ざっている
func UpdatePrices(db *sql.DB) error {
    rows, _ := db.Query("SELECT ...")
    for rows.Next() {
        // ここに複雑な価格計算ロジック
    }
    db.Exec("UPDATE ...")
}

// ✓ 分離されている
// Core：純粋。TDDの対象。0.001秒でテストできる
func CalculateNewPrices(listings []Listing, rates Rates) []PriceUpdate { ... }

// Shell：薄い。統合テストで1本だけ通す
func UpdatePrices(db *sql.DB) error {
    listings, err := fetchListings(db)
    updates := CalculateNewPrices(listings, currentRates)  // ← Coreを呼ぶだけ
    return saveUpdates(db, updates)
}
```

> 💡 **前資料の「型」の話と直結します。** Coreを純粋にすると、入出力が型で表現でき、型で保証できる範囲が広がり、必要なテストが減ります。

#### ⑦ Seam（継ぎ目）があること ※既存コードに導入する場合

新規開発なら⑥だけでよいのですが、**既存コードにTDDを持ち込む場合は追加の前提**があります。

**Feathersのレガシーコードの定義**

> **レガシーコードとは、古いコードでも悪いコードでもない。テストで守られていないために安全に変更できないコードである。**

**レガシーコードのジレンマ**

```
コードを変更するにはテストが必要
        ↕
テストを追加するにはコードを変更する必要がある
```

**解決策は2つ**

**(a) Seam を見つける**

> **Seamとは、その場所を編集することなくプログラムの振る舞いを変更できる場所。**

テストできない理由の **99%は依存の問題**です。DB接続、サードパーティ呼び出し、インスタンス化が複雑な引数——これらを**テストの中で断ち切れる継ぎ目**を探します。

**(b) Characterization Test（仕様化テスト）を書く**

> **Characterization Testは、コードの実際の振る舞いを特徴づけるテスト。** 網羅的なユニットテストを書く代わりに、**現在の振る舞いのスナップショットを取る**。

手順は機械的です。

```
1. メソッドを選ぶ
2. テストから呼び出す
3. 失敗すると分かっているアサーションを書く（例：assertEquals(0, result)）
4. 実行する。失敗メッセージが「実際の戻り値」を教えてくれる
5. アサーションを実際の出力に書き換える。テストが通る
6. 安全網の完成
```

**重要な注意点**：

> 価格計算関数が75を返すと期待して実行したら7500だった。**7500で固定せよ。** それが間違って見えても。振る舞いを承認しているのではなく、**可視化している**のだ。もし7500がバグなら、それは今や**テストを赤くせずには静かに変わりえないバグ**になった。

---

### 12.4 【D】仕事の前提

#### ⑧ 何を作るか分かっていること

**テストは「期待する振る舞い」を書くものなので、期待が定まっていなければ書けません。**

分かっていない場合の正しい対処は、TDDを諦めることではなく **spike（学習用の使い捨て実装）** です。

> **実現可能性を探るためにテストなしでスパイクを打つのは問題ない（「学習スパイク」と呼ぶ）。鍵は、その後に学びをテスト可能な単位へ抽出することだ。**

```
【分からない状態】
        ↓
   spike（テストなし、捨てる前提で書く）
        ↓
   何が難しいか、何が仕様として必要かが分かる ← 理論が育つ
        ↓
   spikeを捨てる
        ↓
   受け入れテストを書く（＝理論の固定）
        ↓
   TDDで作り直す
```

> 💡 これは前資料の **「垂直スライスで理論を育ててから、テストと型に固定する」** と完全に同じ構造です。**spike ＝ 理論を育てる装置**です。

---

### 12.5 環境・技能のチェックリスト

TDDを始める前に、以下に答えてみてください。

| # | 質問 | Yesなら |
|---|---|---|
| 1 | テストは1コマンドで走るか？ | ✅ |
| 2 | ユニットテスト全体が数秒以内に終わるか？ | ✅ |
| 3 | いつでも数分前の状態に戻せるか？ | ✅ |
| 4 | Test-Lastでよいので、テストを書いた経験があるか？ | ✅ |
| 5 | 「関数の抽出」「リネーム」を安全に行えるか？ | ✅ |
| 6 | ビジネスロジックをDB・APIに触れずに呼び出せるか？ | ✅ |
| 7 | （既存コードなら）安全網となるテストが1本でもあるか？ | ✅ |
| 8 | これから作るものの「期待される振る舞い」を言葉にできるか？ | ✅ |

**Noがあった場合の対処**

| No の項目 | 先にやること |
|---|---|
| 1, 2 | **テスト実行環境の整備が最優先。** 遅いテストは別コマンドに分離する |
| 3 | Gitの基本操作に慣れる |
| 4 | **Test-Lastでテストを書く練習を数日する** |
| 5 | IDEの自動リファクタリング機能を使ってみる。Fowler『リファクタリング』を辞書として使う |
| 6 | **Functional Core / Imperative Shell で分離する**（新規部分から） |
| 7 | Characterization Test を1本書いて安全網を作る |
| 8 | **spikeを打つ。** TDDはその後 |

---

### 12.6 「前提」だと誤解されているが、実は不要なもの

準備しすぎて動けなくなるのも失敗です。**以下は前提ではありません。**

| よくある誤解 | 実際 |
|---|---|
| **カバレッジ100%の体制** | 手段の目的化。**壊れても気づけるものはテスト不要** |
| **DIコンテナの導入** | 不要。素朴な引数渡しで十分。むしろ test-induced design damage の温床 |
| **モックライブラリの習熟** | 古典派なら**ほとんど使わない**。自分が制御できるコードはモックしない |
| **完璧なアーキテクチャ** | TDDは設計を**育てる**手法。最初から正しい設計は要らない |
| **プロジェクト全体への一斉適用** | 新規部分・変更部分から始める。既存コード全体にテストを追加する必要はない |
| **チーム全員の合意** | 個人で始められる。まずは自分の担当範囲で |

---

### 12.7 既存プロジェクトに導入するときの順序

Hiroakiさんのように**すでに動いているプロジェクトがある**場合の現実的な手順です。

```
【Week 0】環境を整える
  □ テストコマンドを1つにまとめる
  □ 遅いテストを別コマンドに分離する（例：go test -short）
  □ CIで自動実行されるようにする

【Week 1】安全網を1本だけ作る
  □ 一番重要な機能に Characterization Test を1本書く
  □ 「今の振る舞い」を固定するだけでよい。正しさは問わない

【Week 2】バグ修正でTDDを体験する
  □ バグが出たら、修正前に再現テストを書く
  □ 赤を確認 → 修正 → 緑

【Week 3-4】新規部分を Functional Core として書く
  □ 新しいロジックは純粋関数として切り出す
  □ そこにTDDを適用する

【Month 2以降】受け入れテストを先に書く習慣へ
  □ 新機能で受け入れテストを1本先に書く
  □ 内側ループはLLMに任せる
```

> **既存コード全体をテストで覆う必要はまったくありません。**
> Feathersの言葉を借りれば、**レガシーコードは理解したから良くなるのではなく、Seamを1つずつ作ってテスト可能にすることで良くなる**。小さな変更が積み重なり、数か月後には怖いファイルにテスト済みの核ができています。

---

## 13. 学習の順序と最初の一歩

### 13.1 推奨する学習順序

```
【Step 1】バグ修正でTDDを体験する         ← 最も低コスト
    修正する前に、そのバグを再現する失敗テストを書く
    「何をテストすべきか」が自明なので迷わない
              ↓
【Step 2】純粋関数でRed-Green-Refactorを回す
    価格計算、日付変換、パースなど
    外部依存がないのでモックの悩みが発生しない
              ↓
【Step 3】受け入れテストを1本だけ先に書く
    新機能で。ユニットテストは書かなくていい
    内側ループはLLMに任せる
              ↓
【Step 4】Double Loopを意識する
    外側＝受け入れテスト、内側＝ユニットテスト
              ↓
【Step 5】型で表現できるものを型に移す
    テストの必要量が減っていくのを体感する
```

> **Step 3まで到達すれば、AI協業における最も重要な効果（＝作らせる前に「正しい」を確定させる）は手に入っています。**
> Red-Green-Refactor を厳密に全レベルで回すのは、必要を感じてからで十分です。

### 13.2 最初に避けるべきこと

| 避けること | 理由 |
|---|---|
| **モックを多用するスタイルから始める** | test-induced design damage の主要な発生源 |
| **カバレッジ100%を目標にする** | 手段の目的化。**壊れても気づけるものはテスト不要** |
| **既存の大規模コードにいきなり適用する** | テスト不能な設計と格闘することになる。新規部分から始める |
| **教条的に「テストファーストでなければ偽物」と考える** | 研究上、効いているのは粒度と均一性。挫折の原因になる |

### 13.3 参考書籍

| 書籍 | 位置づけ |
|---|---|
| Kent Beck『**テスト駆動開発**』 | 原典。写経が最も早い。訳者（t_wada）の付録も価値が高い |
| Steve Freeman & Nat Pryce『**Growing Object-Oriented Software, Guided by Tests**』 | ロンドン派の原典。Double Loopの出典 |
| Michael Feathers『**レガシーコード改善ガイド**』 | テストがない既存コードにテストを入れる技法 |

---

## 14. まとめ

### 14.1 TDDとは結局何か

> **TDDは「テスト手法」ではなく「開発のリズムと設計の手法」である。**
> テストは副産物として得られる。主産物は**小さく安全に前進する能力**と、**呼び出し側から駆動された設計**。

### 14.2 事実として押さえるべき4点

| # | 事実 |
|---|---|
| **1** | **品質は上がるが、初期開発時間は伸びる**（欠陥密度40〜90%減 / 時間15〜35%増） |
| **2** | **効いているのはテストファーストそのものではなく、粒度と均一性**である可能性が高い（Fucci et al. 2017） |
| **3** | **test-induced design damage は実在する**。特にモック多用のロンドン派スタイルで発生しやすい |
| **4** | **AI時代に価値が上がった**。エージェントのごまかしを構造的に防げる唯一の実践的手段 |

### 14.3 前資料との接続

この資料は、前資料『AI時代の開発ドキュメント設計ガイド』の以下の主張の**前提知識**にあたります。

| 前資料の主張 | 本資料での裏付け |
|---|---|
| 「テストを先に書く行為」は設計行為である | §1.2（Beckの動機）、§3（0件問題の発見） |
| 実装後に書いたテストはトートロジーになる | §10.1 |
| 内部実装に密結合したテストは負債 | §7.3（test-induced design damage） |
| 粒度が大事 | §8.3（Fucci et al.：粒度と均一性が主要因） |
| 型を効かせるほどテストは減る | §5.4 |
| 受け入れテストだけは人間が握る | §5.3（ATDD/BDD層）、§10.3 |

### 14.4 最後に

TDDは **1990年代に生まれ、2014年に「死んだ」と宣言され、2026年にAIエージェントの登場で再評価されている** という、珍しい経歴を持つ実践です。

この経緯が示しているのは、**TDDが状況依存の道具**だということだと思います。

- **人間だけで書いていた時代**：設計手法としての価値が中心。だから「設計を歪めるなら不要」という批判が成立した
- **AIが書く時代**：**AIの出力を判定する基準**としての価値が加わった。ここは代替手段がほとんどない

なので現時点での妥当な立ち位置は、おそらくこうです。

> **教条的に全部やる必要はない。**
> **ただし「AIに実装させる前に、正しさの定義を自分の手で固定する」という一点だけは、他に代替手段がない。**

---

## 参考文献

### 書籍・原典

- Kent Beck. *Test-Driven Development: By Example* (2003), Addison-Wesley
- Kent Beck. *Extreme Programming Explained: Embrace Change* (2005)
- Steve Freeman, Nat Pryce. *Growing Object-Oriented Software, Guided by Tests* (2009)
- Michael Feathers. *Working Effectively with Legacy Code* (2004) — レガシーコードの定義、Seam、Characterization Test
- Martin Fowler. *Refactoring: Improving the Design of Existing Code*

### 設計・テスト容易性

- Gary Bernhardt. *Boundaries* — Functional Core, Imperative Shell の出典
- functional-architecture.org, *Functional Core, Imperative Shell*
- understandlegacycode.com, *The key points of Working Effectively with Legacy Code*

### 要件・受け入れ基準

- Definition of Ready / Acceptance Criteria に関する実務ガイド（altexsoft, Pega Academy, Hyperdrive Agile ほか）
- EARS（Easy Approach to Requirements Syntax）記法

### 実証研究

- Nagappan, N., Maximilien, E. M., Bhat, T., Williams, L. (2008). *Realizing quality improvement through test driven development: results and experiences of four industrial teams.* Empirical Software Engineering, 13(3)
- Fucci, D., Erdogmus, H., Turhan, B., Oivo, M., Juristo, N. (2017). *A Dissection of the Test-Driven Development Process: Does It Really Matter to Test-First or to Test-Last?* IEEE TSE 43(7), 597–614 / arXiv:1611.05994
- George, B., Williams, L. (2003). *An initial investigation of test driven development in industry.*
- Bhat, T., Nagappan, N. (2006). *Evaluating the efficacy of test-driven development: industrial case studies.*
- Turhan, B. et al. *Making Software* TDD章（エビデンスは mixed、品質改善への中程度の支持）

### 批判

- David Heinemeier Hansson (2014). *TDD is dead. Long live testing.* https://dhh.dk/2014/tdd-is-dead-long-live-testing.html
- David Heinemeier Hansson (2014). *Test-induced design damage.* https://dhh.dk/2014/test-induced-design-damage.html
- "Is TDD Dead?" 対談シリーズ（DHH × Kent Beck × Martin Fowler, 2014）

### AI協業文脈

- *TDFlow: Agentic Workflows for Test Driven Development.* arXiv:2510.23761
- Kent Beck による augmented coding / vibe coding の区別

---

*本資料は2026年7月時点の調査に基づく。TDDの効果に関する実証研究は依然として結論が一致しておらず、研究の厳密性・関連性・被験者の熟練度・タスクの複雑さによって結果が変動することに留意されたい。*
